Introduction to Programming using Python - Lab

1.1 Swap of two numbers
1.2 Find the largest of three numbers
1.3 Given the principal amount, rate of interest, and number of years, calculate both simple interest and compound interest.

2.1 Find the GCD and LCM of two numbers
2.2 Computes the roots of a quadratic function.
2.3 Write a program to find the factorial of the given number (n! = n*n-1*n-2* … * 2* 1)

3.1 Print the “Hello World” n times, where n is any user defined number.
3.2 Print the Pascal triangle of row n, where n is any user defined number.
3.3 Write a program for Bubble sort.

4.1 Write a function to calculate n! recursively.
4.2 Calculate the value of nCr and nPr for a given n and r.
4.3 Given a number, check whether it is prime.

5.1 Add and multiply two matrices.
5.2 Using list, store all the user input names and display in the same ordered as entered.
5.3 Store the content of a list (with duplicate entries) in a set and display the set content.
5.4 Store the student information in a (RollNo, Name) pare in a dictionary and retrieve any name with a given RollNo.
6.1 Using NumPy library perform the matrix operations like addition, multiplication, inverse.
6.2 Solve a set of linear equations (Example, 2x + y = 8 and 3x + 4y = 18).
6.3 Read the content of a .csv file using pandas.
6.4 Read the content of a .csv file (that contains an arbitrary English text) using pandas. Find the frequencies of individual tokens
and store them in another .csv file.

Books
Python Programming: Using Problem Solving Approach by Reema Thareja
Python Programming: An Introduction to Computer Science by John Zelle
Introduction to Python Programming by Gowrishankar S and Veena A

Online Materials:
https://www.kaggle.com/learn/python
https://docs.python.org/3/tutorial/index.html


#[Swap of two numbers]
# 1. The Pythonic Way (Tuple Unpacking)
# Initialize variables
num1 = 5
num2 = 10
print(f"Before swap: num1 = {num1}, num2 = {num2}")
# Swap using tuple unpacking
num1, num2 = num2, num1
print(f"After swap: num1 = {num1}, num2 = {num2}")

# 1.1 The Pythonic Way (Tuple Unpacking)
a = 5
b = 10
# Swapping using tuple unpacking
a, b = b, a
print(f"a: {a}, b: {b}")  # Output: a: 10, b: 5

# 2. Using a Temporary Variable
a = 5
b = 10
# Swapping using a temporary variable
temp = a
a = b
b = temp

#3. Using Arithmetic Operations
a = 5
b = 10
a = a + b  # a becomes 15
b = a - b  # b becomes 5 (original a)
a = a - b  # a becomes 10 (original b)

# 4. Using the XOR Bitwise Operator
a = 5  # Binary: 0101
b = 10 # Binary: 1010
a = a ^ b
b = a ^ b
a = a ^ b

#Find the largest of three numbers
#Using the max() Function
def find_largest_max(num1, num2, num3):
  """Finds the largest of three numbers using the max() function."""
  largest = max(num1, num2, num3)
  return largest
# Example usage
a = 10
b = 45
c = 21
result = find_largest_max(a, b, c)
print(f"The largest number is: {result}")
# Output: The largest number is: 45

#Using if-elif-else Statements
def find_largest_conditional(num1, num2, num3):
  """Finds the largest of three numbers using if-elif-else statements."""
  if (num1 >= num2) and (num1 >= num3):
    largest = num1
  elif (num2 >= num1) and (num2 >= num3):
    largest = num2
  else:
    largest = num3
  return largest
# Example usage
a = 10
b = 45
c = 21
result = find_largest_conditional(a, b, c)
print(f"The largest number is: {result}")
# Output: The largest number is: 45

#Using a List and sort()
def find_largest_sort(num1, num2, num3):
  """Finds the largest of three numbers by sorting a list."""
  numbers = [num1, num2, num3]
  numbers.sort()
  largest = numbers[-1] # The last element is the largest after sorting
  return largest
# Example usage
a = 10
b = 45
c = 21
result = find_largest_sort(a, b, c)
print(f"The largest number is: {result}")
# Output: The largest number is: 45

#Given the principal amount, rate of interest, and number of years, calculate both simple interest and compound interest.
def calculate_simple_interest(principal, rate, years):
    """
    Calculates the Simple Interest (SI).
    Formula: SI = (P * R * T) / 100
    """
    simple_interest = (principal * rate * years) / 100
    return simple_interest
def calculate_compound_interest(principal, rate, years):
    """
    Calculates the Compound Interest (CI) and total amount.
    Formula for total amount: A = P * (1 + R/100)^T
    Formula for CI: CI = A - P
    """
    # Calculate the total amount using the compound interest formula
    amount = principal * (pow((1 + rate / 100), years))
    # Calculate the compound interest (Total Amount - Principal)
    compound_interest = amount - principal
    return compound_interest
def format_currency(amount):
    """Formats the amount to two decimal places."""
    return f"{amount:,.2f}"
# --- Example Usage ---
# Define the inputs
principal_amount = 10000  # P
interest_rate = 5        # R (as a percentage)
time_years = 3           # T
# Calculate the interests
si = calculate_simple_interest(principal_amount, interest_rate, time_years)
ci = calculate_compound_interest(principal_amount, interest_rate, time_years)
# Print the results
print(f"Principal Amount: ${format_currency(principal_amount)}")
print(f"Annual Interest Rate: {interest_rate}%")
print(f"Time Period: {time_years} years\n")
print(f"Simple Interest (SI): ${format_currency(si)}")
print(f"Compound Interest (CI): ${format_currency(ci)}")

#Find the GCD and LCM of two numbers
import math
def calculate_gcd_lcm(a, b):
    # Calculate GCD (Greatest Common Divisor)
    gcd_value = math.gcd(a, b)
    # Calculate LCM (Least Common Multiple)
    # The formula is: LCM(a, b) = (a * b) / GCD(a, b)
    lcm_value = abs(a*b) // math.gcd(a, b)
    return gcd_value, lcm_value
# Example usage
num1 = 56
num2 = 98
gcd, lcm = calculate_gcd_lcm(num1, num2)
print(f"The GCD of {num1} and {num2} is: {gcd}")
print(f"The LCM of {num1} and {num2} is: {lcm}")
#Output:
#The GCD of 56 and 98 is: 14
#The LCM of 56 and 98 is: 392

#Manually calculating GCD using the Euclidean Algorithm
def find_gcd(a, b):
    # Euclidean algorithm
    while b:
        a, b = b, a % b
    return a

def calculate_lcm_manual(a, b):
    # Formula: LCM(a, b) = (a * b) / GCD(a, b)
    # Use floor division (//) for integer results
    lcm = abs(a*b) // find_gcd(a, b)
    return lcm

# Example usage
num1 = 12
num2 = 18
gcd = find_gcd(num1, num2)
lcm = calculate_lcm_manual(num1, num2)
print(f"The GCD of {num1} and {num2} is: {gcd}")
print(f"The LCM of {num1} and {num2} is: {lcm}")
#Output:
#The GCD of 12 and 18 is: 6
#The LCM of 12 and 18 is: 36

#Computes the roots of a quadratic function.
import math
def calculate_quadratic_roots(a, b, c):
    """
    Calculates the roots of a quadratic function ax^2 + bx + c = 0.
    """
    if a == 0:
        if b == 0:
            return "No solution (a and b cannot both be zero)."
        else:
            # Linear equation bx + c = 0
            return f"Linear equation, single root: x = {-c / b}"
    # Calculate the discriminant
    delta = b**2 - 4*a*c
    if delta > 0:
        # Two distinct real roots
        x1 = (-b + math.sqrt(delta)) / (2*a)
        x2 = (-b - math.sqrt(delta)) / (2*a)
        return f"Two distinct real roots: x1 = {x1}, x2 = {x2}"
    elif delta == 0:
        # One real root (or two equal roots)
        x = -b / (2*a)
        return f"One real root: x = {x}"
    else:
        # Two complex roots
        real_part = -b / (2*a)
        imaginary_part = math.sqrt(abs(delta)) / (2*a)
        return f"Two complex roots: x1 = {real_part} + {imaginary_part}i, x2 = {real_part} - {imaginary_part}i"
# Main part of the script for user input
if __name__ == "__main__":
    print("Enter the coefficients for ax^2 + bx + c = 0")
    try:
        a = float(input("Enter a: "))
        b = float(input("Enter b: "))
        c = float(input("Enter c: "))
        result = calculate_quadratic_roots(a, b, c)
        print(f"\nResult: {result}")
    except ValueError:
        print("Invalid input. Please enter numerical values for the coefficients.")

#Write a program to find the factorial of the given number (n! = n*n-1*n-2* … * 2* 1)
#Using a for loop (iterative method)
def factorial_iterative(n):
    """Calculates the factorial of a non-negative integer using a loop."""
    if n < 0:
        return "Factorial is not defined for negative numbers"
    elif n == 0 or n == 1:
        return 1
    else:
        result = 1
        for i in range(1, n + 1):
            result *= i
        return result
# Example usage:
number = 5
print(f"The factorial of {number} is: {factorial_iterative(number)}")
number = 0
print(f"The factorial of {number} is: {factorial_iterative(number)}")
#Output:
#The factorial of 5 is: 120
#The factorial of 0 is: 1

#Using recursion
def factorial_recursive(n):
    """Calculates the factorial of a non-negative integer using recursion."""
    if n < 0:
        return "Factorial is not defined for negative numbers"
    elif n == 0 or n == 1:
        return 1
    else:
        return n * factorial_recursive(n - 1)
# Example usage:
number = 6
print(f"The factorial of {number} is: {factorial_recursive(number)}")
#The factorial of 6 is: 720

#Using the math module (built-in function) 
import math
def factorial_builtin(n):
    """Calculates the factorial of a non-negative integer using the math module."""
    if n < 0:
        return "Factorial is not defined for negative numbers"
    else:
        return math.factorial(n)
# Example usage:
number = 7
print(f"The factorial of {number} is: {factorial_builtin(number)}")
#Output:
#The factorial of 7 is: 5040

this is a practical set of questions...

more on its way coming up.
